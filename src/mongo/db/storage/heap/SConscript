# -*- mode: python -*-

Import("env")

env.Library(
    target= 'heap_storage_engine',
    source= [
        'heap_recovery_unit.cpp',
        ],
    LIBDEPS= [
        '$BUILD_DIR/mongo/bson'
        ]
    )
